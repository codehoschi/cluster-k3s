---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: apply-cluster-config
  namespace: flux-system
spec:
  dependsOn:
    - name: apply-manage-flux
  interval: 10m0s
  path: ./k8s/clusters/hekate/flux/vars
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: cluster-k3s
  decryption:
    provider: sops
    secretRef:
      name: sops-age
